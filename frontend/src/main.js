import Vue from 'vue'
import App from './App'
// import Schedule from './components/Schedule'
// import ScheduleStrip from './components/ScheduleStrip'

// Vue.component('schedule', Schedule)
// Vue.component('schedule-strip', ScheduleStrip)

/* eslint-disable no-new */
new Vue({
  el: 'body',
  components: { App }
})
